# Perform one isolated Hegotá snapshot complexity assessment: EIP-8151

This is a source template. The trusted coordinator must render the assigned EIP number and title into `PROMPT.md` inside one isolated capsule. Never run this prompt directly from the research repository.

You are assessing exactly EIP-8151, Account Code Restricted ecRecover, as it appears in the Hegotá PFI snapshot sealed in `package/`.

Read `ASSESSMENT-CONTRACT.md` completely. Then read `package/manifest.yaml`, `package/rubric.md`, `package/eip.md`, `package/output-template.yaml`, and every file listed in `manifest.yaml` under `assessment_source_files`. Do not read any other file.

Evaluate only the proposal state and evidence inside the package. For a cross-layer proposal, assess only its execution-layer surface. Apply every anchor in `package/rubric.md` in exact order, including anchors scored zero. Do not add criteria, repair missing details from memory, or infer how Hegotá will ultimately implement or scope the proposal.

For each anchor provide one permitted score, one or more structured package-relative evidence locators, a concise technical rationale tied to the anchor, confidence, an uncertainty note, and an exceptional-score justification when required. Record material under-specification separately and do not multiply one missing detail across unrelated anchors.

The internet, web search, browsers, network commands, apps, plugins, MCP servers, skills, the repository, project-management records, other proposals or packages, prior scores, human assessments, calibration data, implementations, tests, devnets, client discussions, post-snapshot revisions, fork decisions, and outcomes are prohibited. A visible URL is provenance only and must not be opened. Use latent knowledge neither to fill specification gaps nor as scoring evidence.

If prohibited information is exposed, follow the contamination procedure in `ASSESSMENT-CONTRACT.md` and do not produce an authoritative result.

Copy `package/output-template.yaml` to `assessment.yaml` and fill every required field. Preserve all launcher-populated provenance, source lists, operational-file lists, and session values exactly. Calculate the total and tier mechanically using only the sealed rubric.

Write no file other than `/mnt/workspace/assessment.yaml`. Do not run repository validators, copy the result elsewhere, or inspect aggregate state. Finish after `assessment.yaml` is complete; the trusted launcher validates and collects it after the process exits.
