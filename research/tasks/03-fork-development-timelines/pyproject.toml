[project]
name = "retrospective-fork-development-timelines"
version = "0.1.0"
description = "Reproducible EL, CL, and fork-level EIP development timelines"
requires-python = ">=3.11,<3.14"
dependencies = [
  "altair>=6.0,<7",
  "cairosvg>=2.8,<3",
  "pyyaml>=6.0,<7",
  "vl-convert-python>=1.8,<2",
]

[tool.uv]
package = false
