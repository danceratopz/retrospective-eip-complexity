# Task 02 outputs

Each EIP agent writes exactly one file:

```text
eip-<number>.yaml
```

Copy the structure from `../templates/output.yaml`. Do not place downloaded source files, charts, fork-level summaries, or mixed-EIP datasets in this directory.
