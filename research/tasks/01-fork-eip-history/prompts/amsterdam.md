# Agent prompt: Amsterdam

Research the Amsterdam/Glamsterdam fork for Task 01.

1. Read `research/tasks/01-fork-eip-history/TASK.md` completely.
2. Use only `research/tasks/01-fork-eip-history/inputs/amsterdam.yaml` as the fork-level input.
3. Follow `templates/fork-output.yaml`, `templates/fork-eip-output.yaml`, and `templates/source-registry-output.yaml`.
4. Write the Amsterdam fork record, its fork–EIP records, `outputs/sources/forks/amsterdam.yaml`, and permitted mutable snapshots under `raw/forks/amsterdam/`.
5. Do not modify the task contract, templates, inputs, prompts, or another fork's outputs.
6. Do not score EIP complexity or choose retrospective assessment dates.

EIP-7773 is a moving Draft and is the authoritative membership source. The inventory is its Scheduled for Inclusion section at the resolved source commit. Use its Declined section and Git history to reconstruct earlier inclusion cycles, but do not add declined or merely contextual EIPs to the fork inventory.

Client implementation history and devnet participation are out of scope for this run.

Do not inspect or write global EIP revision histories; separate EIP workers own `outputs/eips/`.
