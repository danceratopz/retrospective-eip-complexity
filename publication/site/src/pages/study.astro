---
import Layout from '../layouts/Layout.astro';
import { basePath } from '../lib/data.js';
---
<Layout title="Study moved" redirect={basePath()}>
  <h1>Study Moved</h1>
  <p>The study is now the <a href={basePath()}>site homepage</a>.</p>
</Layout>
